import React from "react";

import { SectionComponents } from "../../../components";

export const ImageTextButton= ({
  subTitle,
  title,
  description,
  buttonName,
  photo,
  photoMobileSource,
  settings,
}) => {

}