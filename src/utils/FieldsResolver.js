export const FieldsResolver = () => {

  const textField = ({field}) => {
    return field.value
  }

  const uploadField = ({field, isArray, photoType='compression'}) => {
    
    const getSource = 
  }
}